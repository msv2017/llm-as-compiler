# 04 — Sort

**DSL feature:** `Sort`.

**Prompt:** "Given a customer id, list their invoices sorted by amount, highest first, and return
the sorted list."

No existing test in this repo exercised `SortNode` before this example.
