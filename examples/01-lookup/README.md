# 01 — Lookup

**DSL feature:** `Call`, `Return` — the simplest possible workflow shape.

**Prompt:** "Given a customer id, look up and return the customer's name."

One tool call, one field copied into the result. See `workflow.txt` for the compiled shape,
`sample-input.json` / `run-output.txt` for a real execution against the demo server.
